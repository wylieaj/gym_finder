<%- layout('layouts/boilerplate.ejs') %>
<div class="row justify-contents-start">
  <% for (let plan of gym.plans) { %>
  <div class="d-flex justify-content-center mb-3 col-sm-6 col-md-4 col-lg-3 col-xxl-2">
    <form action="/dashboard/gyms/<%= gym._id %>/plans/<%= plan._id %>?_method=DELETE" method="POST">
    <div class="card" style="width: 18rem; height: 18rem">
      <div class="card-header text-center">
        <h5 class="card-title mb-0 py-3"><%= plan.name %></h5>
      </div>
      <div class="card-body d-flex align-items-center justify-content-center">
        <p class="mb-0"><%= plan.description %></p>
      </div>
      <div class="card-footer d-flex justify-content-between align-items-center">
        <div>
          <p class="mb-0">£<%= plan.price %> Per <%= plan.planType %></p>
        </div>
        <div>
          <button class="btn btn-danger btn-sm">Delete</button>
        </div>
        </form>
      </div>
    </div>
  </div>
  <% } %>

  <div class="d-flex justify-content-center mb-3 col-sm-6 col-md-4 col-lg-3 col-xxl-2">
    <a href="/dashboard/gyms/<%= gym._id %>/plans/new" class="text-decoration-none">
      <div class="card bg-light text-secondary align-items-center justify-content-center" style="width: 18rem; height: 18rem">
        <h1 class="display-1">&plus;</h1>
      </div>
    </a>
  </div>
</div>

<div>
  <a href="/dashboard/gyms" class="btn btn-secondary">Back</a>
</div>
